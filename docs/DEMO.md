# AI Product Factory Demo

The Product Factory Studio takes a plain-language product idea through research, evidence-backed planning, approval, autonomous source generation, executable verification, a running product preview and a downloadable source ZIP.

The short repository demo clip is stored at `docs/media/ai-product-factory-demo.mp4` and is linked from the main README.

For the full interactive experience, run the project and open `/studio`.
