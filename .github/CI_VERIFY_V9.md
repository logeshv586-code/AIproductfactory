# AI Product Factory v9 CI verification

This branch exists only to trigger the full pull-request CI workflow against the current `main` implementation after the approved-composition flow changes.

The production changes are already on `main`; this file is not intended to be merged.
